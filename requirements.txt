fastapi
uvicorn[standard]

# LangChain - create_tool_calling_agent 호환 버전 (검증됨)
langchain==0.3.7
langchain-community==0.3.7
langchain-openai==0.3.4
langchain-ollama==0.2.3

chromadb

SpeechRecognition==3.10.0
pydub

opencv-python

huggingface-hub<0.26.0, >=0.15.0
transformers<5.0.0
imgbeddings==0.1.0
facenet-pytorch
mediapipe
python-multipart

numpy<2.0.0

# ROS2 dependencies
rclpy
# cv_bridge - ROS2 system package required
# Installation: sudo apt install ros-humble-cv-bridge (for ROS2 Humble)

# FFmpeg for audio processing (if using conda/pip)
# Note: System installation may be required: sudo apt install ffmpeg
